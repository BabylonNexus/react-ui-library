# React UI Library
## SETUP

To setup project run 
```sh
npm install
```
In case of peer-depedency error just run with --force flag
```sh
npm install --force
```
## START

To start project just run 
```sh
npm run storybook
